{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The NetworkCore desktop window may invoke only its Rust command bridge.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
