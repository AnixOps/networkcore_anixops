[package]
name = "mitm-anixops-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Unsafe C ABI bindings for the vendored mitm_anixops core."
links = "mitm_anixops"

[build-dependencies]
cc = "1"

[lib]
path = "src/lib.rs"
